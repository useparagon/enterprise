apiVersion: v1
kind: Secret
metadata:
  name: paragon-secrets
  namespace: {{ .Values.namespace | default "paragon" }}
type: Opaque
stringData:
  {{- range $key, $val := .Values.secrets }}
  {{ $key }}: {{ $val | quote }}
  {{- end }} 
