apiVersion: v1
kind: Service
metadata:
  name: {{ include "redis-stream-exporter.fullname" . }}
  labels:
    {{- include "redis-stream-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "redis-stream-exporter.selectorLabels" . | nindent 4 }}
