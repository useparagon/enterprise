apiVersion: v1
kind: Service
metadata:
  name: {{ include "worker-triggerkit.fullname" . }}
  labels:
    {{- include "worker-triggerkit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "worker-triggerkit.selectorLabels" . | nindent 4 }}
